Model implementation in R:

1. Fork project code from https://github.com/arildsd/ML-FIFA19-Models into an IDE that supports R.

2. Make sure that the sufficient libraries are installed (should be clear from the code)

3. Run code (May be necessary to use the debugger if run in IntelliJ)